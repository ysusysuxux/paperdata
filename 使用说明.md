# MomoL-VL 攻击脚本使用说明

## 🎯 修复内容总结

### 1. 修复了模型名称错误
- **问题**: 代码中硬编码模型名称为 `Qwen2.5-VL-72B-Instruct`，但 vLLM 部署的模型名称是 `momolVL`
- **解决**: 添加了 `model_name` 参数，默认值为 `momolVL`，与部署命令中的模型路径匹配

### 2. 修复了 API 端点和格式
- **问题**: 使用了错误的 API 端点和请求格式
- **解决**: 
  - 端点改为 `/invocations`（默认端口 8000）
  - 使用测试成功的格式 3b: `{prompt, image_data}`

### 3. 添加了批量处理功能
- **新增**: `--type` 参数支持批量处理 JSONL 文件
- **功能**: 自动读取 JSONL 文件，提取 `new_prompt` 和 `img_path`，批量调用模型

---

## 📋 使用方法

### 方式一：批量处理 JSONL 文件（推荐）

```bash
# 处理 01-Illegal_Activitiy.jsonl
python attack.py --type 01 --server http://localhost:8000

# 处理 03
