# 多模态 VL 攻击脚本使用说明

## 🎯 功能特性

### 1. 支持多种模型
- **MomoL** - 使用格式: `{prompt, image_data}`
- **InternVL3-78B** - 使用格式: OpenAI 风格 messages

### 2. 自动选择正确的 API 格式
- 根据 `--model` 参数自动选择对应的请求格式
- 自动处理不同模型的响应格式

### 3. 批量处理功能
- **新增**: `--type` 参数支持批量处理 JSONL 文件
- **功能**: 自动读取 JSONL 文件，提取 `new_prompt` 和 `img_path`，批量调用模型
- **输出**: 自动生成包含模型名称的结果文件

---

## 📋 使用方法

### 方式一：批量处理 JSONL 文件（推荐）

#### 使用 MomoL 模型
```bash
# 处理 01-Illegal_Activitiy.jsonl（使用 momol 模型）
python attack.py --type 01 --model momol --server http://localhost:8000

# 处理 06-Fraud.jsonl
python attack.py --type 06 --model momol --server http://localhost:8000
# 输出文件: 06-Fraud-results-momol.json
```

#### 使用 InternVL 模型
```bash
# 处理 01-Illegal_Activitiy.jsonl（使用 internvl 模型）
python attack.py --type 01 --model internvl --server http://localhost:8000

# 处理 03-Malware_Generation.jsonl  
python attack.py --type 03 --model internvl --server http://localhost:8000 --max_tokens 4096
# 输出文件: 03-Malware_Generation-results-internvl.json
```

### 方式二：单个文本和图片推理

#### 使用 MomoL 模型
```bash
python attack.py --text "这是什么?" --image_path "image.jpg" --model momol
# 输出文件: eomol-momol.json
```

#### 使用 InternVL 模型
```bash
python attack.py --text "分析这张图片" --image_path "test.png" --model internvl
# 输出文件: eomol-internvl.json
```

---

## 📁 输出文件命名规则

### 批量处理模式
格式: `{base_name}-results-{model}.json`

示例:
- `01-Illegal_Activitiy-results-momol.json`
- `01-Illegal_Activitiy-results-internvl.json`
- `06-Fraud-results-momol.json`
- `06-Fraud-results-internvl.json`

### 单个推理模式
格式: `eomol-{model}.json`

示例:
- `eomol-momol.json`
- `eomol-internvl.json`

---

## 🚀 部署模型

### 部署 MomoL 模型
```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
vllm serve /mnt/nfs/evo/momolVL \
    --tensor-parallel-size 4 \
    --max-num-seqs 16 \
    --gpu-memory-utilization 0.70 \
    --trust-remote-code \
    --disable-custom-all-reduce \
    --max-model-len 4096
```

### 部署 InternVL 模型
```bash
CUDA_VISIBLE_DEVICES=1,3 \
vllm serve /mnt/nfs/lq/internVL \
    --tensor-parallel-size 2 \
    --max-model-len 4096 \
    --max-num-seqs 16 \
    --gpu-memory-utilization 0.98 \
    --trust-remote-code \
    --disable-custom-all-reduce
```

---

## 📊 批量处理类型 (--type)

| 类型 | JSONL 文件 | 描述 |
|------|-----------|------|
| 01 | 01-Illegal_Activitiy.jsonl | 非法活动 |
| 02 | 02-HateSpeech.jsonl.jsonl | 仇恨言论 |
| 03 | 03-Malware_Generation.jsonl | 恶意软件生成 |
| 04 | 04-Physical_Harme.jsonl | 物理伤害 |
| 05 | 05-EconomicHarm.jsonl | 经济伤害 |
| 06 | 06-Fraud.jsonl | 欺诈 |
| 07 | 07-Sex.jsonl | 性相关 |

---

## 🔧 可选参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--server` | http://localhost:8000 | vLLM 服务器地址 |
| `--model` | momol | 模型类型 (momol/internvl) |
| `--max_tokens` | 2048 | 最大生成长度 |
| `--temperature` | 0.7 | 温度参数 |
| `--top_p` | 0.9 | Top-p 采样参数 |
| `--output` | 自动生成 | 自定义输出文件路径 |

---

## 💡 完整示例

### 示例 1: 使用 MomoL 批量处理所有类型
```bash
for i in 01 02 03 04 05 06 07; do
    python attack.py --type $i --model momol --server http://localhost:8000
done
```

### 示例 2: 使用 InternVL 批量处理所有类型
```bash
for i in 01 02 03 04 05 06 07; do
    python attack.py --type $i --model internvl --server http://localhost:8000
done
```

### 示例 3: 同时处理两个模型（需要两个服务器）
```bash
# 假设 MomoL 在 8000 端口，InternVL 在 8001 端口
python attack.py --type 01 --model momol --server http://localhost:8000 &
python attack.py --type 01 --model internvl --server http://localhost:8001 &
wait
```

---

## ❓ 常见问题

### Q: 如何查看模型是否成功部署？
```bash
curl http://localhost:8000/v1/models
```

### Q: 两个模型可以同时运行吗？
A: 可以，但需要使用不同的 GPU 和端口。建议使用不同的 `CUDA_VISIBLE_DEVICES` 和 `--port` 参数。

### Q: 输出文件已存在怎么办？
A: 脚本会以追加模式保存，不会覆盖现有结果。

### Q: 如何继续未完成的批量处理？
A: 脚本支持断点续传，自动跳过已处理的记录（如果输出文件存在）。

---

## 📝 模型对比

| 特性 | MomoL | InternVL3-78B |
|------|-------|---------------|
| 参数量 | - | 78B |
| API 格式 | {prompt, image_data} | OpenAI messages |
| 响应格式 | text_completion | chat.completion |
| 推荐用途 | 快速推理 | 高质量推理 |

---

## 🎉 完成

现在你可以：
1. ✅ 使用 `--model momol` 或 `--model internvl` 选择模型
2. ✅ 使用 `--type 01-07` 批量处理不同类型的数据
3. ✅ 自动生成包含模型名称的结果文件
4. ✅ 两个模型使用相同的输入数据，结果分开保存
