# 更新总结 - 支持双模型

## ✅ 完成的修改

### 1. `run.py` 修改
- ✅ 将 `model_name` 改为 `model_type`
- ✅ 支持 `momol` 和 `internvl` 两种模型类型
- ✅ 根据模型类型自动选择正确的 API 格式：
  - **momol**: `{prompt, image_data}` 格式
  - **internvl**: OpenAI 风格 messages 格式
- ✅ 优化响应解析，优先处理 Chat Completion 格式

### 2. `attack.py` 修改
- ✅ 添加 `--model` 参数（choices: momol, internvl）
- ✅ 批量模式输出文件自动包含模型名称
  - 例如: `06-Fraud-results-internvl.json`
- ✅ 单个模式输出文件也包含模型名称
  - 例如: `eomol-internvl.json`
- ✅ 传递 `model_type` 参数到 MomoLVLInferencer

### 3. 文档创建
- ✅ 创建 `test_intern_api.py` - InternVL 测试脚本
- ✅ 创建 `使用说明.md` - 完整使用指南
- ✅ 创建 `InternVL测试说明.md` - InternVL 测试指南

---

## 🎯 关键变更

### API 格式对比

#### MomoL 格式（格式 3b）
```python
payload = {
    'prompt': text,
    'image_data': image_base64,
    'max_tokens': max_tokens,
    'temperature': temperature,
    'top_p': top_p
}
# 端点: /invocations
# 响应: {"choices": [{"text": "..."}]}
```

#### InternVL 格式（格式 9）
```python
payload = {
    'messages': [
        {
            'role': 'user',
            'content': [
                {'type': 'text', 'text': text},
                {'type': 'image_url', 'image_url': {'url': f'data:image/jpeg;base64,{image_base64}'}}
            ]
        }
    ],
    'max_tokens': max_tokens,
    'temperature': temperature,
    'top_p': top_p
}
# 端点: /invocations
# 响应: {"choices": [{"message": {"content": "..."}}]}
```

---

## 📝 使用示例

### 测试 InternVL 部署
```bash
python test_intern_api.py
# 查看哪种格式成功（应该是格式 9）
```

### 单个推理
```bash
# MomoL 模型
python attack.py --text "描述图片" --image_path "test.jpg" --model momol

# InternVL 模型
python attack.py --text "描述图片" --image_path "test.jpg" --model internvl
```

### 批量处理
```bash
# 使用 MomoL 处理类型 01
python attack.py --type 01 --model momol --server http://localhost:8000
# 输出: 01-Illegal_Activitiy-results-momol.json

# 使用 InternVL 处理类型 01
python attack.py --type 01 --model internvl --server http://localhost:8000
# 输出: 01-Illegal_Activitiy-results-internvl.json
```

### 批量处理所有类型
```bash
# MomoL 模型
for i in 01 02 03 04 05 06 07; do
    python attack.py --type $i --model momol --server http://localhost:8000
done

# InternVL 模型
for i in 01 02 03 04 05 06 07; do
    python attack.py --type $i --model internvl --server http://localhost:8000
done
```

---

## 🔄 迁移指南

### 从旧版本迁移

#### 旧命令（不支持 model 参数）
```bash
python attack.py --type 01 --server http://localhost:8000
```

#### 新命令（需要指定 model）
```bash
# 使用 momol（默认）
python attack.py --type 01 --model momol --server http://localhost:8000

# 使用 internvl
python attack.py --type 01 --model internvl --server http://localhost:8000
```

---

## 📊 输出文件对比

### 旧版本
```
01-Illegal_Activitiy-results.json
```

### 新版本
```
01-Illegal_Activitiy-results-momol.json
01-Illegal_Activitiy-results-internvl.json
```

这样两个模型的结果完全分开，便于对比。

---

## ✨ 优势

1. **统一接口** - 两个模型使用相同的命令行参数
2. **自动格式** - 根据模型自动选择正确的 API 格式
3. **结果分离** - 不同模型的结果保存到不同文件
4. **易于对比** - 可以方便地对比两个模型的输出
5. **向后兼容** - 保留了所有原有功能

---

## 🧪 验证步骤

### 1. 验证 run.py
```bash
# 测试 momol
python run.py --text "测试" --image "test.jpg" --model momol

# 测试 internvl
python run.py --text "测试" --image "test.jpg" --model internvl
```

### 2. 验证 attack.py 批量模式
```bash
# 测试 momol
python attack.py --type 01 --model momol --server http://localhost:8000

# 测试 internvl
python attack.py --type 01 --model internvl --server http://localhost:8000
```

### 3. 检查输出文件
```bash
ls -la *-results-*.json
# 应该看到：
# 01-Illegal_Activitiy-results-momol.json
# 01-Illegal_Activitiy-results-internvl.json
```

---

## 🎉 完成！

现在你的系统支持：
- ✅ MomoL 和 InternVL 两种模型
- ✅ 自动选择正确的 API 格式
- ✅ 结果文件包含模型名称
- ✅ 统一的命令行接口
- ✅ 完整的文档说明

